server:
  port: 8081

spring:
  application:
    name: auth-service   # 🔥 VERY IMPORTANT

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER_URL}
    register-with-eureka: true
    fetch-registry: true

  instance:
    prefer-ip-address: true

